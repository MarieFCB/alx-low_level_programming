#include <string.h>
#include <stdio.h>
#include "main.h"

/**
 * _strncpy - copy a string
 * at most n bytes of src are copied
 * @n: the maximum byte to copy
 * @src: the string to copy n byte from
 * @dest: the string where to paste what is copied
 */
char *_strncpy(char *dest, char *src, int n)
{
}
