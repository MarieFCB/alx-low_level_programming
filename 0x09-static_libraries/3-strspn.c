#include "main.h"
#include <stdio.h>

/**
 * _strspn - compute the length (in bytes) of the maximum
 * initial segment of the string pointed to by s which consist
 * entirely of bytes from the string pointed to by accept
 * @s: first string
 * @accept: second string
 * Return: return the length of s
 */
unsigned int _strspn(char *s, char *accept)
{
}
