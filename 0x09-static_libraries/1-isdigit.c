#include <stdio.h>
#include "main.h"

/**
 * _isdigit - checks for a digit (0 through 9)
 * @c: the digit int to check
 * Return: 1 if the character fall in the tested class
 * and 0 value if not
 */
int _isdigit(int c)
{
}
